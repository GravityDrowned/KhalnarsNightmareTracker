return {
    enabled = true,
    alwaysShow = false,
    unlocked = false,
    size = 64,
    positionLeft = GuiRoot:GetWidth() / 2,
    positionTop = GuiRoot:GetHeight() / 2 + 200,
    debugMode = false,
}
